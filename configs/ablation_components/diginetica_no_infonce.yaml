# Ablation: no_infonce | dataset: diginetica
# lambda_proto=0.0, mask_prob=0.05

data:
  dataset: diginetica
  data_dir: ~/data/diginetica_unified/cold_20
  cold_ratio: 20
  expected_cold: -1

model:
  hidden_size: 128
  n_gnn_steps: 1

ablation: A11

train:
  epochs: 30
  batch_size: 100
  lr: 0.001
  weight_decay: 1.0e-5
  grad_clip: 5.0
  patience: 5

catprocl:
  ema_momentum: 0.99
  lambda_proto: 0.0
  temperature: 0.1
  perturbation_std: 0.05
  mask_prob: 0.05

eval:
  ks: [10, 20]
  eval_every: 1

seed: 42
output_dir: ~/results_ablation/diginetica/no_infonce
checkpoint_dir: ~/checkpoints_ablation/diginetica/no_infonce
log_dir: ~/logs_ablation/diginetica/no_infonce

device: cuda
num_workers: 6
use_amp: false
